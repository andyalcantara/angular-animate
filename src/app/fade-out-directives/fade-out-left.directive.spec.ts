import { FadeOutLeftDirective } from './fade-out-left.directive';

describe('FadeOutLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeOutLeftDirective();
    expect(directive).toBeTruthy();
  });
});
