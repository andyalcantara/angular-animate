import { FadeOutUpDirective } from './fade-out-up.directive';

describe('FadeOutUpDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeOutUpDirective();
    expect(directive).toBeTruthy();
  });
});
