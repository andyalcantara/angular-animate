import { FadeOutUpBigDirective } from './fade-out-up-big.directive';

describe('FadeOutUpBigDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeOutUpBigDirective();
    expect(directive).toBeTruthy();
  });
});
