import { FadeOutRightDirective } from './fade-out-right.directive';

describe('FadeOutRightDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeOutRightDirective();
    expect(directive).toBeTruthy();
  });
});
