import { FadeOutLeftBigDirective } from './fade-out-left-big.directive';

describe('FadeOutLeftBigDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeOutLeftBigDirective();
    expect(directive).toBeTruthy();
  });
});
