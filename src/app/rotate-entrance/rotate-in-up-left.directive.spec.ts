import { RotateInUpLeftDirective } from './rotate-in-up-left.directive';

describe('RotateInUpLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateInUpLeftDirective();
    expect(directive).toBeTruthy();
  });
});
