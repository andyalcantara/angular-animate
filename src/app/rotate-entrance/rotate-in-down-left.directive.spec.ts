import { RotateInDownLeftDirective } from './rotate-in-down-left.directive';

describe('RotateInDownLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateInDownLeftDirective();
    expect(directive).toBeTruthy();
  });
});
