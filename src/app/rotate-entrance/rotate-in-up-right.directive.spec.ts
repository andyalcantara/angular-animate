import { RotateInUpRightDirective } from './rotate-in-up-right.directive';

describe('RotateInUpRightDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateInUpRightDirective();
    expect(directive).toBeTruthy();
  });
});
