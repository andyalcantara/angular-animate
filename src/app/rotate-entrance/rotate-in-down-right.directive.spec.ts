import { RotateInDownRightDirective } from './rotate-in-down-right.directive';

describe('RotateInDownRightDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateInDownRightDirective();
    expect(directive).toBeTruthy();
  });
});
