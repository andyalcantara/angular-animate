import { RotateInDirective } from './rotate-in.directive';

describe('RotateInDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateInDirective();
    expect(directive).toBeTruthy();
  });
});
