import { BounceOutDirective } from './bounce-out.directive';

describe('BounceOutDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceOutDirective();
    expect(directive).toBeTruthy();
  });
});
