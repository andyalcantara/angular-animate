import { BounceOutLeftDirective } from './bounce-out-left.directive';

describe('BounceOutLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceOutLeftDirective();
    expect(directive).toBeTruthy();
  });
});
