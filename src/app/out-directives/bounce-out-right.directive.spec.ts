import { BounceOutRightDirective } from './bounce-out-right.directive';

describe('BounceOutRightDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceOutRightDirective();
    expect(directive).toBeTruthy();
  });
});
