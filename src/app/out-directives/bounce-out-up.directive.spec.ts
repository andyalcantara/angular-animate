import { BounceOutUpDirective } from './bounce-out-up.directive';

describe('BounceOutUpDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceOutUpDirective();
    expect(directive).toBeTruthy();
  });
});
