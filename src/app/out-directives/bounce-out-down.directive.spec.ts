import { BounceOutDownDirective } from './bounce-out-down.directive';

describe('BounceOutDownDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceOutDownDirective();
    expect(directive).toBeTruthy();
  });
});
