import { FlipOutYDirective } from './flip-out-y.directive';

describe('FlipOutYDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipOutYDirective();
    expect(directive).toBeTruthy();
  });
});
