import { FlipInYDirective } from './flip-in-y.directive';

describe('FlipInYDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipInYDirective();
    expect(directive).toBeTruthy();
  });
});
