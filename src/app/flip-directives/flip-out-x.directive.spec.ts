import { FlipOutXDirective } from './flip-out-x.directive';

describe('FlipOutXDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipOutXDirective();
    expect(directive).toBeTruthy();
  });
});
