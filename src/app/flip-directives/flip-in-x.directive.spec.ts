import { FlipInXDirective } from './flip-in-x.directive';

describe('FlipInXDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipInXDirective();
    expect(directive).toBeTruthy();
  });
});
