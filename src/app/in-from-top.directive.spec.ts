import { InFromTopDirective } from './in-from-top.directive';

describe('InFromTopDirective', () => {
  it('should create an instance', () => {
    const directive = new InFromTopDirective();
    expect(directive).toBeTruthy();
  });
});
