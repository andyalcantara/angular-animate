import { BounceInDownDirective } from './bounce-in-down.directive';

describe('BounceInDownDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceInDownDirective();
    expect(directive).toBeTruthy();
  });
});
