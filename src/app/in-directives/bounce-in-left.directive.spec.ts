import { BounceInLeftDirective } from './bounce-in-left.directive';

describe('BounceInLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceInLeftDirective();
    expect(directive).toBeTruthy();
  });
});
