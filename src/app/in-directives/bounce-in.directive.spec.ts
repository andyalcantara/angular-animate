import { BounceInDirective } from './bounce-in.directive';

describe('BounceInDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceInDirective();
    expect(directive).toBeTruthy();
  });
});
