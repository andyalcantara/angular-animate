import { BounceInUpDirective } from './bounce-in-up.directive';

describe('BounceInUpDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceInUpDirective();
    expect(directive).toBeTruthy();
  });
});
