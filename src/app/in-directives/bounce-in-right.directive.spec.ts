import { BounceInRightDirective } from './bounce-in-right.directive';

describe('BounceInRightDirective', () => {
  it('should create an instance', () => {
    const directive = new BounceInRightDirective();
    expect(directive).toBeTruthy();
  });
});
