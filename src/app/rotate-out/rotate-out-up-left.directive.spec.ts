import { RotateOutUpLeftDirective } from './rotate-out-up-left.directive';

describe('RotateOutUpLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateOutUpLeftDirective();
    expect(directive).toBeTruthy();
  });
});
