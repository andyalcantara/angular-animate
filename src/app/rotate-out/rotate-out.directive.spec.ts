import { RotateOutDirective } from './rotate-out.directive';

describe('RotateOutDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateOutDirective();
    expect(directive).toBeTruthy();
  });
});
