import { RotateOutDownRightDirective } from './rotate-out-down-right.directive';

describe('RotateOutDownRightDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateOutDownRightDirective();
    expect(directive).toBeTruthy();
  });
});
