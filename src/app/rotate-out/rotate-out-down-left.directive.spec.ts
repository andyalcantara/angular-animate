import { RotateOutDownLeftDirective } from './rotate-out-down-left.directive';

describe('RotateOutDownLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateOutDownLeftDirective();
    expect(directive).toBeTruthy();
  });
});
