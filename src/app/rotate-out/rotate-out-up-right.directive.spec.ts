import { RotateOutUpRightDirective } from './rotate-out-up-right.directive';

describe('RotateOutUpRightDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateOutUpRightDirective();
    expect(directive).toBeTruthy();
  });
});
