import { TadaDirective } from './tada.directive';

describe('TadaDirective', () => {
  it('should create an instance', () => {
    const directive = new TadaDirective();
    expect(directive).toBeTruthy();
  });
});
