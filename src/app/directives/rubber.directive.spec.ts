import { RubberDirective } from './rubber.directive';

describe('RubberDirective', () => {
  it('should create an instance', () => {
    const directive = new RubberDirective();
    expect(directive).toBeTruthy();
  });
});
