import { JelloDirective } from './jello.directive';

describe('JelloDirective', () => {
  it('should create an instance', () => {
    const directive = new JelloDirective();
    expect(directive).toBeTruthy();
  });
});
