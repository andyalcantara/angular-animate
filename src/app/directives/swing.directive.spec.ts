import { SwingDirective } from './swing.directive';

describe('SwingDirective', () => {
  it('should create an instance', () => {
    const directive = new SwingDirective();
    expect(directive).toBeTruthy();
  });
});
