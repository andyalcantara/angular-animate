import { ShakeAtHoverDirective } from './shake-at-hover.directive';

describe('ShakeAtHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ShakeAtHoverDirective();
    expect(directive).toBeTruthy();
  });
});
