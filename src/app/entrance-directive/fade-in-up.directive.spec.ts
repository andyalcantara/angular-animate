import { FadeInUpDirective } from './fade-in-up.directive';

describe('FadeInUpDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInUpDirective();
    expect(directive).toBeTruthy();
  });
});
