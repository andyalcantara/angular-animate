import { FadeInRightBigDirective } from './fade-in-right-big.directive';

describe('FadeInRightBigDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInRightBigDirective();
    expect(directive).toBeTruthy();
  });
});
