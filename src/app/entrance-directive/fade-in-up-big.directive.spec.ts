import { FadeInUpBigDirective } from './fade-in-up-big.directive';

describe('FadeInUpBigDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInUpBigDirective();
    expect(directive).toBeTruthy();
  });
});
