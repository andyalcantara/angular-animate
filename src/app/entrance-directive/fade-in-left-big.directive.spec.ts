import { FadeInLeftBigDirective } from './fade-in-left-big.directive';

describe('FadeInLeftBigDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInLeftBigDirective();
    expect(directive).toBeTruthy();
  });
});
