import { FadeInLeftDirective } from './fade-in-left.directive';

describe('FadeInLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInLeftDirective();
    expect(directive).toBeTruthy();
  });
});
