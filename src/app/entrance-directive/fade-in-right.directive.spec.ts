import { FadeInRightDirective } from './fade-in-right.directive';

describe('FadeInRightDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInRightDirective();
    expect(directive).toBeTruthy();
  });
});
