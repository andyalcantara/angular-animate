import { FadeInDownBigDirective } from './fade-in-down-big.directive';

describe('FadeInDownBigDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInDownBigDirective();
    expect(directive).toBeTruthy();
  });
});
