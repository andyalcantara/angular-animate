import { FadeInDownDirective } from './fade-in-down.directive';

describe('FadeInDownDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInDownDirective();
    expect(directive).toBeTruthy();
  });
});
