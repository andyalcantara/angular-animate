import { MouseClickDirective } from './mouse-click.directive';

describe('MouseClickDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseClickDirective();
    expect(directive).toBeTruthy();
  });
});
