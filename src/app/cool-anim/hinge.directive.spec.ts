import { HingeDirective } from './hinge.directive';

describe('HingeDirective', () => {
  it('should create an instance', () => {
    const directive = new HingeDirective();
    expect(directive).toBeTruthy();
  });
});
