import { RollInDirective } from './roll-in.directive';

describe('RollInDirective', () => {
  it('should create an instance', () => {
    const directive = new RollInDirective();
    expect(directive).toBeTruthy();
  });
});
