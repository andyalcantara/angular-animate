import { RollOutDirective } from './roll-out.directive';

describe('RollOutDirective', () => {
  it('should create an instance', () => {
    const directive = new RollOutDirective();
    expect(directive).toBeTruthy();
  });
});
