import { AttrFromTopDirective } from './attr-from-top.directive';

describe('AttrFromTopDirective', () => {
  it('should create an instance', () => {
    const directive = new AttrFromTopDirective();
    expect(directive).toBeTruthy();
  });
});
