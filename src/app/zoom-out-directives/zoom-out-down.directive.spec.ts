import { ZoomOutDownDirective } from './zoom-out-down.directive';

describe('ZoomOutDownDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomOutDownDirective();
    expect(directive).toBeTruthy();
  });
});
