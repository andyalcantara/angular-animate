import { ZoomOutLeftDirective } from './zoom-out-left.directive';

describe('ZoomOutLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomOutLeftDirective();
    expect(directive).toBeTruthy();
  });
});
