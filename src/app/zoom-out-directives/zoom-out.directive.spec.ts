import { ZoomOutDirective } from './zoom-out.directive';

describe('ZoomOutDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomOutDirective();
    expect(directive).toBeTruthy();
  });
});
