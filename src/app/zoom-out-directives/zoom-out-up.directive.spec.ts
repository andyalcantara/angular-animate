import { ZoomOutUpDirective } from './zoom-out-up.directive';

describe('ZoomOutUpDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomOutUpDirective();
    expect(directive).toBeTruthy();
  });
});
