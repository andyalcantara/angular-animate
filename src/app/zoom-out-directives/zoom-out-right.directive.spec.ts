import { ZoomOutRightDirective } from './zoom-out-right.directive';

describe('ZoomOutRightDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomOutRightDirective();
    expect(directive).toBeTruthy();
  });
});
