import { SlideInRightDirective } from './slide-in-right.directive';

describe('SlideInRightDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideInRightDirective();
    expect(directive).toBeTruthy();
  });
});
