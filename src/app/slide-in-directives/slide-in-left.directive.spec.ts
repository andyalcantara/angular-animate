import { SlideInLeftDirective } from './slide-in-left.directive';

describe('SlideInLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideInLeftDirective();
    expect(directive).toBeTruthy();
  });
});
