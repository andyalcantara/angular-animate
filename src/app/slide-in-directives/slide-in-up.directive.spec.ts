import { SlideInUpDirective } from './slide-in-up.directive';

describe('SlideInUpDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideInUpDirective();
    expect(directive).toBeTruthy();
  });
});
