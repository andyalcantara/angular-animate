import { SlideInDownDirective } from './slide-in-down.directive';

describe('SlideInDownDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideInDownDirective();
    expect(directive).toBeTruthy();
  });
});
