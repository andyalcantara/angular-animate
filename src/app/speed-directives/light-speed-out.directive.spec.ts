import { LightSpeedOutDirective } from './light-speed-out.directive';

describe('LightSpeedOutDirective', () => {
  it('should create an instance', () => {
    const directive = new LightSpeedOutDirective();
    expect(directive).toBeTruthy();
  });
});
