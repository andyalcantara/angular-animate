import { LightSpeedInDirective } from './light-speed-in.directive';

describe('LightSpeedInDirective', () => {
  it('should create an instance', () => {
    const directive = new LightSpeedInDirective();
    expect(directive).toBeTruthy();
  });
});
