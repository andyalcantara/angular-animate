import { ZoomInLeftDirective } from './zoom-in-left.directive';

describe('ZoomInLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomInLeftDirective();
    expect(directive).toBeTruthy();
  });
});
