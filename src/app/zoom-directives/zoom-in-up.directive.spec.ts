import { ZoomInUpDirective } from './zoom-in-up.directive';

describe('ZoomInUpDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomInUpDirective();
    expect(directive).toBeTruthy();
  });
});
