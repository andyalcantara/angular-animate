import { ZoomInRightDirective } from './zoom-in-right.directive';

describe('ZoomInRightDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomInRightDirective();
    expect(directive).toBeTruthy();
  });
});
