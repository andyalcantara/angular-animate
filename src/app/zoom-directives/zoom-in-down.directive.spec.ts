import { ZoomInDownDirective } from './zoom-in-down.directive';

describe('ZoomInDownDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomInDownDirective();
    expect(directive).toBeTruthy();
  });
});
